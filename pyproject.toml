[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dcus2023-video-tracking"
version = "0.1.0"
description = "Tracks YouTube video view counts for the DjangoConUS channel"
requires-python = ">=3.12"
dependencies = [
    "datasette",
    "yt-dlp",
]
